import React from 'react'

const Button = () => {
    const width = 100;
    const height = 100;
    const variant = 'primary'
    const onClick = 'onclick'

  return (
    <button>

    </button>
  )
}

export default Button