export const getUser = () =>{

}

export const userIsAllowed = (user, rolesAllowed) => {
    if(!user || !rolesAllowed)
        return false;
    // if user has a role that is allowed return true
    return false;
}